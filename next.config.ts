import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This repo is an SDLC demo — steering files are written by hand, not generated.
  agentRules: false,
};

export default nextConfig;
